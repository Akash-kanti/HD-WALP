import 'package:flutter/material.dart';

PreferredSizeWidget hdAppBar(){
  return AppBar(
    backgroundColor: Colors.deepPurple,
    title: Center(child: Text("HD WALP", style: TextStyle(color: Colors.white, fontSize: 25, fontWeight: FontWeight.bold),)),
    titleSpacing: 0.00,

  );
}